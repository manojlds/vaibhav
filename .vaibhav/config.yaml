# vaibhav ralph configuration
# Auto-generated — edit as needed

project:
  name: "vaibhav"
  language: "Bash"

commands:
  lint: "shellcheck bin/vaibhav bin/vaibhav-ralph"
  # test: ""
  # build: ""

rules:
  - "use set -euo pipefail in all scripts"
  - "follow existing color/output conventions (step/ok/warn helpers)"
  - "keep scripts POSIX-compatible where possible, bash where needed"
  - "match existing coding style in bin/vaibhav"

boundaries:
  never_touch:
    - "*.lock"
    - ".env*"
    - "prd.json"
    - "progress.txt"

engine: "amp"
max_retries: 3
